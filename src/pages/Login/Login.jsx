import React from 'react';
import "./Login.scss";

const Login = ({darkMode, setDarkMode}) => {
    return (
        <div>
            <h1>This is logiin</h1>
        </div>
    );
};

export default Login;